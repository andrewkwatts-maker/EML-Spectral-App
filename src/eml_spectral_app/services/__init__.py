"""Domain services for EML-Spectral-App.

The screen layer depends on these via small abstractions (a parser, a
formatter, a hit-tester) so each can be swapped or extended without
touching the view code (SOLID DIP + OCP).
"""
